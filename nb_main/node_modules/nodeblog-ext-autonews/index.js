"use strict";

var path = require('path');
var router = require(path.join(LIBS_DIR, 'router'))();

module.exports = function() {
    var self = {};

    self.registerStaticRoutes = function(app) {
        console.log('hook on before dynamic router initialized');
    };

    return self;
};
